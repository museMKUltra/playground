spring:
  application:
    name: playground
  datasource:
    url: jdbc:mysql://localhost:3306/playground?createDatabaseIfNotExist=true
    username: root
    password: MyPassword!
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}